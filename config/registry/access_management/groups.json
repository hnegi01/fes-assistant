[
  {
    "tool_id": "access_management.create_groups_bulk",
    "description": "Create multiple groups in a single bulk request.",
    "parameters": {
      "type": "object",
      "properties": {
        "groups": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Group definitions to create. Each dictionary should use canonical Sisense group fields, at minimum ``name``."
        }
      },
      "required": [
        "groups"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Create multiple groups for the new project team: 'Data Analysts', 'Project Managers', and 'QA Engineers'.",
        "arguments": {
          "groups": [
            {
              "name": "Data Analysts"
            },
            {
              "name": "Project Managers"
            },
            {
              "name": "QA Engineers"
            }
          ]
        },
        "notes": "This call creates three new groups in bulk for organizing users in the new project team."
      },
      {
        "user_query": "Can you bulk-create groups for 'Marketing Leads' and 'Sales Executives'?",
        "arguments": {
          "groups": [
            {
              "name": "Marketing Leads"
            },
            {
              "name": "Sales Executives"
            }
          ]
        },
        "notes": "Use this call to quickly create multiple groups for department-specific roles."
      },
      {
        "user_query": "I need to set up groups for 'HR Admins', 'Finance Team', and 'IT Support'.",
        "arguments": {
          "groups": [
            {
              "name": "HR Admins"
            },
            {
              "name": "Finance Team"
            },
            {
              "name": "IT Support"
            }
          ]
        },
        "notes": "This example demonstrates creating groups for different organizational functions in one request."
      }
    ]
  },
  {
    "tool_id": "access_management.delete_group",
    "description": "Delete a group by ID.",
    "parameters": {
      "type": "object",
      "properties": {
        "group_id": {
          "type": "string",
          "description": "The ID of the group to delete."
        }
      },
      "required": [
        "group_id"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Delete the group with ID 'group_12345'.",
        "arguments": {
          "group_id": "group_12345"
        },
        "notes": "This call deletes the group identified by 'group_12345' from the Sisense server. Use this when you need to remove a group permanently."
      },
      {
        "user_query": "How do I remove a group with the ID 'marketing_team'?",
        "arguments": {
          "group_id": "marketing_team"
        },
        "notes": "This example demonstrates how to delete a group named 'marketing_team'. Use this when you want to clean up unused or obsolete groups."
      },
      {
        "user_query": "Can you delete the group identified as 'archived_group_001'?",
        "arguments": {
          "group_id": "archived_group_001"
        },
        "notes": "This call removes the group 'archived_group_001'. This is useful for managing archived or deprecated groups in your system."
      }
    ]
  },
  {
    "tool_id": "access_management.get_group",
    "description": "Retrieve group details by name.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the group to be retrieved."
        }
      },
      "required": [
        "name"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Show the details of the 'Data Analysts' group.",
        "arguments": {
          "name": "Data Analysts"
        },
        "notes": "This retrieves the details of the 'Data Analysts' group, including its ID, name, and default role. Use this to verify group configurations or permissions."
      },
      {
        "user_query": "Can you fetch information about the 'Admin Team' group?",
        "arguments": {
          "name": "Admin Team"
        },
        "notes": "This call retrieves the details of the 'Admin Team' group. Useful for checking the group's default role and ensuring it is set up correctly."
      },
      {
        "user_query": "I need to look up the 'Marketing Users' group to confirm its default role.",
        "arguments": {
          "name": "Marketing Users"
        },
        "notes": "This retrieves the 'Marketing Users' group details, which can help confirm its default role or troubleshoot access issues."
      }
    ]
  },
  {
    "tool_id": "access_management.get_groups",
    "description": "Retrieve all groups.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Retrieve all groups from the Sisense server.",
        "arguments": {},
        "notes": "This call fetches the complete list of groups defined on the Sisense server. Useful for auditing or managing group memberships."
      },
      {
        "user_query": "What are all the groups currently defined in Sisense?",
        "arguments": {},
        "notes": "This call retrieves all groups, which can be used to review existing group configurations or permissions."
      },
      {
        "user_query": "List all groups available on the Sisense server.",
        "arguments": {},
        "notes": "Use this call to get a comprehensive list of groups for administrative purposes or integration tasks."
      }
    ]
  },
  {
    "tool_id": "access_management.users_per_group",
    "description": "Retrieve all users within a specific group by name.",
    "parameters": {
      "type": "object",
      "properties": {
        "group_name": {
          "type": "string",
          "description": "The name of the group whose members to list."
        }
      },
      "required": [
        "group_name"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "List the members of the 'Data Analysts' group.",
        "arguments": {
          "group_name": "Data Analysts"
        },
        "notes": "This call retrieves all users belonging to the 'Data Analysts' group. Use it to audit group membership or fetch user details for further processing."
      },
      {
        "user_query": "List all users in the 'Executive Team' group.",
        "arguments": {
          "group_name": "Executive Team"
        },
        "notes": "Use this call to fetch the members of the 'Executive Team' group, which might include high-level stakeholders or decision-makers."
      },
      {
        "user_query": "Get the list of users in the 'Marketing Specialists' group.",
        "arguments": {
          "group_name": "Marketing Specialists"
        },
        "notes": "This call is helpful for identifying members of the 'Marketing Specialists' group, enabling targeted communication or permissions management."
      }
    ]
  },
  {
    "tool_id": "access_management.users_per_group_all",
    "description": "Retrieve all groups mapped to the users belonging to them.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Show all groups with the users belonging to each.",
        "arguments": {},
        "notes": "This call retrieves all groups and their associated users, excluding system-wide groups like 'Everyone' and 'All users in system'. Useful for auditing user-group mappings."
      },
      {
        "user_query": "Can I get a list of all groups and the users assigned to them?",
        "arguments": {},
        "notes": "This call returns a detailed mapping of groups to users, including empty groups and special roles like 'Admins'. Ideal for understanding group membership across the system."
      },
      {
        "user_query": "Show me all groups and their members, but exclude system-wide groups.",
        "arguments": {},
        "notes": "This call provides a filtered list of groups and their members, excluding predefined system-wide groups. Use this for focused group membership analysis."
      }
    ]
  }
]