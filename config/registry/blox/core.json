[
  {
    "tool_id": "blox.delete_blox_action",
    "description": "Delete a custom Blox action from the Sisense instance.",
    "parameters": {
      "type": "object",
      "properties": {
        "action_type": {
          "type": "string",
          "description": "The ``type`` identifier of the Blox action to delete."
        }
      },
      "required": [
        "action_type"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How do I delete the custom Blox action for exporting dashboard data?",
        "arguments": {
          "action_type": "export_dashboard_data"
        },
        "notes": "This call deletes the custom Blox action identified by 'export_dashboard_data'. Use this if the action is no longer needed or needs to be replaced."
      },
      {
        "user_query": "I need to remove the Blox action for sending email notifications. How can I do that?",
        "arguments": {
          "action_type": "send_email_notification"
        },
        "notes": "This call removes the custom Blox action with the type 'send_email_notification'. Use this to clean up unused or outdated actions."
      },
      {
        "user_query": "Can I delete the Blox action for generating PDF reports?",
        "arguments": {
          "action_type": "generate_pdf_report"
        },
        "notes": "This call deletes the custom Blox action identified as 'generate_pdf_report'. Use this if the action is obsolete or causing issues."
      }
    ]
  },
  {
    "tool_id": "blox.get_blox_actions",
    "description": "Retrieve all custom Blox actions from the Sisense instance.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What custom Blox actions are available in my Sisense instance?",
        "arguments": {},
        "notes": "This call retrieves all custom Blox actions configured in the Sisense instance. Use it to review available actions for automation or customization."
      },
      {
        "user_query": "Can I get a list of all custom actions created for Blox?",
        "arguments": {},
        "notes": "This call provides a list of all custom Blox actions. Useful for administrators managing or auditing Blox configurations."
      },
      {
        "user_query": "Show me the custom Blox actions I can use for my dashboards.",
        "arguments": {},
        "notes": "This call fetches all custom Blox actions, which can be integrated into dashboards for enhanced functionality."
      }
    ]
  },
  {
    "tool_id": "blox.save_blox_action",
    "description": "Save a custom Blox action on the Sisense instance.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "object",
          "description": "A Blox action object."
        }
      },
      "required": [
        "action"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How can I save a new custom action for my dashboard filters?",
        "arguments": {
          "action": {
            "name": "ApplyFilterAction",
            "type": "filter",
            "description": "Applies a predefined filter to the dashboard.",
            "payload": {
              "filterId": "12345",
              "dashboardId": "67890"
            }
          }
        },
        "notes": "This call saves a custom action named 'ApplyFilterAction' that applies a specific filter to a dashboard. Use this to streamline repetitive filtering tasks."
      },
      {
        "user_query": "I want to create a custom action to export dashboard data to a CSV file. How can I do that?",
        "arguments": {
          "action": {
            "name": "ExportToCSV",
            "type": "export",
            "description": "Exports dashboard data to a CSV file.",
            "payload": {
              "dashboardId": "54321",
              "fileFormat": "csv",
              "includeFilters": true
            }
          }
        },
        "notes": "This call creates a custom action named 'ExportToCSV' that allows users to export data from a specific dashboard to a CSV file. Useful for sharing data externally."
      },
      {
        "user_query": "Can I save a custom action to send an email notification when a widget is updated?",
        "arguments": {
          "action": {
            "name": "NotifyOnWidgetUpdate",
            "type": "notification",
            "description": "Sends an email notification when a widget is updated.",
            "payload": {
              "widgetId": "98765",
              "emailRecipients": [
                "user1@example.com",
                "user2@example.com"
              ],
              "subject": "Widget Update Notification",
              "message": "The widget has been updated. Please review the changes."
            }
          }
        },
        "notes": "This call saves a custom action named 'NotifyOnWidgetUpdate' that sends an email notification to specified recipients whenever a particular widget is updated. Ideal for keeping stakeholders informed."
      }
    ]
  }
]